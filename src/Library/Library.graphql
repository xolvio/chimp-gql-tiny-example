# A library has a branch and books
type Library {
  id: ID!
  branch: String!
  books: [Book!]!
}

# A book has a title
type Book {
  id: ID!
  title: String!
  library: Library!
}

type Query {
  libraries: [Library!]!
}
